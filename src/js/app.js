alert("TU VAS BIEN ICI");
const bonjour = (nom) => `Salut ${nom}`;
const hello = (name) => `Salut ${name}`;

window.addEventListener("DOMContentLoaded", function () {
  let x = bonjour("Gilbert");
  let y = hello("Gulp User");
  console.log(x, y);
});
